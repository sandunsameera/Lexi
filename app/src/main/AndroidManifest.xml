<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lolin.deemon_face.lexi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".Lexi"
        android:theme="@style/AppTheme">
        <activity android:name=".DeleteActivity"></activity>
        <activity android:name=".UpdateActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".AddCater" />
        <activity android:name=".AddDecorator" />
        <activity android:name=".AddPhotographer" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menuActivity" />
        <activity
            android:name=".wedding"
            android:label="@string/title_activity_wedding"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".birthday"
            android:label="@string/title_activity_birthday"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".beachParty"
            android:label="@string/title_activity_beach_party"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>